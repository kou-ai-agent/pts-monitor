name: Test Japannext CSV Access

on:
  workflow_dispatch:
  schedule:
    - cron: '53 5 * * *'
      timezone: 'Asia/Tokyo'
    - cron: '3,13,23,33,43,53 6,7,8 * * *'
      timezone: 'Asia/Tokyo'
    - cron: '3 9 * * *'
      timezone: 'Asia/Tokyo'

jobs:
  test-fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch CSV and record log line
        run: |
          TS=$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M:%S JST')
          TRIGGER="${{ github.event_name }}"
          HTTP_CODE=$(curl -sS -o /tmp/japannext.csv -w "%{http_code}" "https://www.japannext.co.jp/csv_download/dnd_top_ranking/NGHT")
          DATE_LINE=$(sed -n '2p' /tmp/japannext.csv | tr -d '\r')
          echo "${TS} | trigger=${TRIGGER} | http=${HTTP_CODE} | ${DATE_LINE}" >> japannext_watch_log.txt

      - name: Commit and push with retry
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add japannext_watch_log.txt
          git commit -m "test: japannext watch log entry [skip ci]" || echo "no changes to commit"
          for i in 1 2 3; do
            if git push; then
              echo "push succeeded on attempt $i"
              break
            fi
            echo "push failed, attempt $i/3. pulling and retrying..."
            git pull --rebase origin HEAD
            sleep 5
          done
